{
  "name": "@cts-dc-opsdesk/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript enums/DTOs shared between api, worker and web",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo \"no lint config yet\" && exit 0",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
