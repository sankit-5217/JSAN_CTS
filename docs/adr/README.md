# Architecture Decision Records

These record the decisions the build spec already mandates as baseline
(Appendix B). Each is pre-filled as **Accepted** since the spec treats them
as non-negotiable guardrails, not open proposals — update status to
**Superseded** with a new ADR if the team formally revisits one.
